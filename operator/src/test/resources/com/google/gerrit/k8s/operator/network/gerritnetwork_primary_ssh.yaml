apiVersion: "gerritoperator.google.com/v1beta19"
kind: GerritNetwork
metadata:
  name: gerrit
  namespace: gerrit
spec:
  ingress:
    enabled: true
    host: example.com
    tls:
      enabled: false
    ssh:
      enabled: true
    istio:
      mTLS: true
  primaryGerrit:
    name: primary
    httpPort: 48080
    sshPort: 49418
