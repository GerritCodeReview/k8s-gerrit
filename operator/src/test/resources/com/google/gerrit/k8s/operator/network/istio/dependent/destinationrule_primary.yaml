apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: primary
  namespace: gerrit
spec:
  host: primary-service.gerrit.svc.cluster.local
  trafficPolicy:
    loadBalancer:
      consistentHash:
        useSourceIp: true
