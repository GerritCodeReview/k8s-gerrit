apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: replica
  namespace: gerrit
spec:
  host: replica-service.gerrit.svc.cluster.local
  trafficPolicy:
    loadBalancer:
      simple: LEAST_CONN
