apiVersion: "gerritoperator.google.com/v1beta1"
kind: GerritNetwork
metadata:
  name: gerrit
  namespace: gerrit
spec:
  ingress:
    enabled: true
    host: example.com
    tls:
      enabled: false
  primaryGerrit:
    name: primary
    httpPort: 48080
    sshPort: 49418
